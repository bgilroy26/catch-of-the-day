var React = require('react');
var ReactDOM = require('react-dom');

/*
 * StorePicker
 * This will let ups make <StorePicker/>
 */

var StorePicker = React.createClass({
    render: function(){
        return (
            <p>hi</p>
            )
    }
});

ReactDOM.render(<StorePicker/>)
